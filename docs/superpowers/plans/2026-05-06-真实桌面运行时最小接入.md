# 真实桌面运行时最小接入实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为前端原型新增默认运行时选择、最小 `tauriRuntime`、目录环境校验和设置页运行时状态展示。

**Architecture:** `src/runtime` 继续作为运行时边界，新增元数据类型、Tauri bridge、Tauri runtime 和 runtime factory。Store 默认通过 factory 获取 runtime，组件只消费 Store 暴露的状态和 action，不直接访问 Tauri API。

**Tech Stack:** Vue 3, TypeScript, Vite, Vitest, Vue Test Utils, browser `localStorage`.

---

## File Structure

- Modify: `src/runtime/types.ts`  
  增加 `RuntimeMode`、`RuntimeEnvironmentStatus`、`DirectoryCheckResult`，并让 `GenerationRuntime` 必须提供 `getEnvironmentStatus(settings)`。
- Modify: `src/runtime/mockRuntime.ts`  
  为 mock runtime 增加运行时状态返回。
- Modify: `src/runtime/mockRuntime.test.ts`  
  覆盖 mock 环境状态。
- Create: `src/runtime/tauriBridge.ts`  
  定义 `TauriDesktopBridge` 和默认桥接实现，隔离全局 Tauri API 检测。
- Create: `src/runtime/tauriRuntime.ts`  
  基于 bridge 和 mock runtime 实现最小桌面 runtime。
- Create: `src/runtime/tauriRuntime.test.ts`  
  覆盖 Tauri 不可用、空目录、不可写目录和可用目录。
- Create: `src/runtime/runtimeFactory.ts`  
  在 `tauriRuntime` 可用后，默认根据 bridge 可用性选择 `tauriRuntime` 或 `mockRuntime`。
- Create: `src/runtime/runtimeFactory.test.ts`  
  在 `tauriRuntime` 可用后，覆盖默认选择逻辑。
- Modify: `src/stores/appStore.ts`  
  默认改用 runtime factory，新增 `runtimeStatus` 和 `refreshRuntimeStatus()`，生成前合并 runtime 校验错误。
- Modify: `src/stores/appStore.test.ts`  
  覆盖运行时状态刷新和生成前目录校验阻断。
- Modify: `src/components/SettingsView.vue`  
  增加运行时状态区域和重新检测按钮。
- Modify: `src/components/SettingsView.test.ts`  
  覆盖运行时模式、错误和重新检测按钮展示。
- Modify: `src/styles.css`  
  增加轻量运行时状态样式。
- Modify: `README.md`  
  说明最小桌面 runtime 的当前能力和限制。
- Rename: `docs/superpowers/specs/2026-05-06-soft-copyright-tauri-runtime-minimum-design.md` -> `docs/superpowers/specs/2026-05-06-真实桌面运行时最小接入设计.md`  
  满足 `AGENTS.md` 的中文文档命名要求。

## Task 1: Runtime Metadata

**Files:**
- Modify: `src/runtime/types.ts`
- Modify: `src/runtime/mockRuntime.ts`
- Modify: `src/runtime/mockRuntime.test.ts`

- [ ] **Step 1: Add failing tests for runtime metadata**

Add to `src/runtime/mockRuntime.test.ts`:

```ts
it('reports browser mock runtime status', async () => {
  const runtime = createMockRuntime()
  const status = await runtime.getEnvironmentStatus(createInitialSettings())

  expect(status.mode).toBe('mock')
  expect(status.label).toBe('浏览器模拟运行时')
  expect(status.available).toBe(true)
  expect(status.issues).toEqual([])
})
```

- [ ] **Step 2: Run tests and confirm failure**

Run:

```bash
npm run test -- src/runtime/mockRuntime.test.ts
```

Expected: FAIL because `getEnvironmentStatus` does not exist.

- [ ] **Step 3: Extend runtime types**

Update `src/runtime/types.ts`:

```ts
export type RuntimeMode = 'mock' | 'tauri'

export interface RuntimeEnvironmentStatus {
  mode: RuntimeMode
  label: string
  available: boolean
  checkedAt: string
  issues: RuntimeErrorInfo[]
}

export interface DirectoryCheckResult {
  exists: boolean
  writable: boolean
  message?: string
}
```

Add to `GenerationRuntime`:

```ts
getEnvironmentStatus(settings: AppSettings): Promise<RuntimeEnvironmentStatus>
```

- [ ] **Step 4: Implement mock status**

In `src/runtime/mockRuntime.ts`, add this method inside the returned object:

```ts
async getEnvironmentStatus() {
  return {
    mode: 'mock',
    label: '浏览器模拟运行时',
    available: true,
    checkedAt: nowText(),
    issues: []
  }
}
```

- [ ] **Step 5: Run runtime metadata verification**

Run:

```bash
npm run test -- src/runtime/mockRuntime.test.ts
npm run build
```

Expected: PASS.

- [ ] **Step 6: Commit runtime metadata and factory**

Run:

```bash
git add src/runtime/types.ts src/runtime/mockRuntime.ts src/runtime/mockRuntime.test.ts
git commit -m "feat: add runtime mode metadata"
```

## Task 2: Minimal Tauri Runtime

**Files:**
- Create: `src/runtime/tauriBridge.ts`
- Create: `src/runtime/tauriRuntime.ts`
- Create: `src/runtime/tauriRuntime.test.ts`
- Create: `src/runtime/runtimeFactory.ts`
- Create: `src/runtime/runtimeFactory.test.ts`

- [ ] **Step 1: Add failing Tauri runtime tests**

Create `src/runtime/tauriRuntime.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import { createInitialSettings } from '../domain/mockData'
import { createTauriRuntime } from './tauriRuntime'
import type { TauriDesktopBridge } from './tauriBridge'

function createBridge(result: { exists: boolean; writable: boolean; message?: string }, available = true): TauriDesktopBridge {
  return {
    isAvailable: () => available,
    checkDirectory: async () => result
  }
}

describe('tauri runtime', () => {
  it('reports unavailable when the desktop bridge is missing', async () => {
    const runtime = createTauriRuntime(createBridge({ exists: false, writable: false }, false))
    const status = await runtime.getEnvironmentStatus(createInitialSettings())

    expect(status.mode).toBe('tauri')
    expect(status.available).toBe(false)
    expect(status.issues[0].code).toBe('runtime-tauri-unavailable')
  })

  it('blocks validation when output directory is empty', async () => {
    const runtime = createTauriRuntime(createBridge({ exists: true, writable: true }))
    const issues = await runtime.validateEnvironment(createInitialSettings())

    expect(issues[0].code).toBe('runtime-directory-empty')
  })

  it('blocks validation when output directory is readonly', async () => {
    const settings = createInitialSettings()
    settings.basic.outputDirectory = 'D:/readonly'
    const runtime = createTauriRuntime(createBridge({ exists: true, writable: false, message: 'readonly' }))

    const issues = await runtime.validateEnvironment(settings)

    expect(issues[0].code).toBe('runtime-directory-readonly')
  })

  it('allows validation when output directory exists and is writable', async () => {
    const settings = createInitialSettings()
    settings.basic.outputDirectory = 'D:/fr-outputs'
    const runtime = createTauriRuntime(createBridge({ exists: true, writable: true }))

    await expect(runtime.validateEnvironment(settings)).resolves.toEqual([])
  })
})
```

Create `src/runtime/runtimeFactory.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import { createInitialSettings } from '../domain/mockData'
import { createDefaultRuntime } from './runtimeFactory'
import type { TauriDesktopBridge } from './tauriBridge'

describe('runtime factory', () => {
  it('uses mock runtime when desktop bridge is unavailable', async () => {
    const runtime = createDefaultRuntime({
      isAvailable: () => false,
      checkDirectory: async () => ({ exists: false, writable: false })
    })
    const status = await runtime.getEnvironmentStatus(createInitialSettings())

    expect(status.mode).toBe('mock')
  })

  it('uses tauri runtime when desktop bridge is available', async () => {
    const bridge: TauriDesktopBridge = {
      isAvailable: () => true,
      checkDirectory: async () => ({ exists: true, writable: true })
    }
    const settings = createInitialSettings()
    settings.basic.outputDirectory = 'D:/fr-outputs'

    const runtime = createDefaultRuntime(bridge)
    const status = await runtime.getEnvironmentStatus(settings)

    expect(status.mode).toBe('tauri')
  })
})
```

- [ ] **Step 2: Run tests and confirm failure**

Run:

```bash
npm run test -- src/runtime/tauriRuntime.test.ts
```

Expected: FAIL because `tauriRuntime.ts`, `tauriBridge.ts`, and `runtimeFactory.ts` do not exist.

- [ ] **Step 3: Implement bridge**

Create `src/runtime/tauriBridge.ts`:

```ts
import type { DirectoryCheckResult } from './types'

export interface TauriDesktopBridge {
  isAvailable(): boolean
  checkDirectory(path: string): Promise<DirectoryCheckResult>
}

export function createDefaultTauriBridge(): TauriDesktopBridge {
  return {
    isAvailable() {
      return typeof window !== 'undefined' && '__TAURI_INTERNALS__' in window
    },
    async checkDirectory() {
      return {
        exists: false,
        writable: false,
        message: '桌面目录校验命令尚未接入。'
      }
    }
  }
}
```

- [ ] **Step 4: Implement tauri runtime**

Create `src/runtime/tauriRuntime.ts`:

```ts
import type { AppSettings, RuntimeErrorInfo } from '../domain/types'
import { createMockRuntime } from './mockRuntime'
import type { TauriDesktopBridge } from './tauriBridge'
import type { GenerationRuntime } from './types'

export function createTauriRuntime(bridge: TauriDesktopBridge): GenerationRuntime {
  const fallback = createMockRuntime()

  return {
    ...fallback,
    async getEnvironmentStatus(settings) {
      const issues = await validateTauriEnvironment(settings, bridge)
      return {
        mode: 'tauri',
        label: '桌面运行时',
        available: bridge.isAvailable() && issues.length === 0,
        checkedAt: nowText(),
        issues
      }
    },
    async validateEnvironment(settings) {
      return validateTauriEnvironment(settings, bridge)
    }
  }
}

async function validateTauriEnvironment(settings: AppSettings, bridge: TauriDesktopBridge): Promise<RuntimeErrorInfo[]> {
  if (!bridge.isAvailable()) {
    return [createError('runtime-tauri-unavailable', '当前未检测到桌面运行时能力。')]
  }

  const outputDirectory = settings.basic.outputDirectory.trim()
  if (!outputDirectory) {
    return [createError('runtime-directory-empty', '请先配置产出资源目录。')]
  }

  const result = await bridge.checkDirectory(outputDirectory)
  if (!result.exists) {
    return [createError('runtime-directory-unavailable', result.message ?? '产出资源目录不可访问。')]
  }

  if (!result.writable) {
    return [createError('runtime-directory-readonly', result.message ?? '产出资源目录不可写。')]
  }

  return []
}

function createError(code: string, message: string): RuntimeErrorInfo {
  return { code, message, recoverable: true }
}

function nowText(): string {
  return new Date().toISOString()
}
```

Create `src/runtime/runtimeFactory.ts`:

```ts
import { createMockRuntime } from './mockRuntime'
import { createTauriRuntime } from './tauriRuntime'
import { createDefaultTauriBridge, type TauriDesktopBridge } from './tauriBridge'
import type { GenerationRuntime } from './types'

export function createDefaultRuntime(
  bridge: TauriDesktopBridge = createDefaultTauriBridge()
): GenerationRuntime {
  if (bridge.isAvailable()) {
    return createTauriRuntime(bridge)
  }
  return createMockRuntime()
}
```

- [ ] **Step 5: Run Tauri runtime verification**

Run:

```bash
npm run test -- src/runtime/tauriRuntime.test.ts src/runtime/runtimeFactory.test.ts src/runtime/mockRuntime.test.ts
npm run build
```

Expected: PASS.

- [ ] **Step 6: Commit minimal Tauri runtime**

Run:

```bash
git add src/runtime/tauriBridge.ts src/runtime/tauriRuntime.ts src/runtime/tauriRuntime.test.ts src/runtime/runtimeFactory.ts src/runtime/runtimeFactory.test.ts
git commit -m "feat: add minimal tauri runtime"
```

## Task 3: Store Runtime Status Integration

**Files:**
- Modify: `src/stores/appStore.ts`
- Modify: `src/stores/appStore.test.ts`

- [ ] **Step 1: Add failing Store tests**

Add to `src/stores/appStore.test.ts`:

```ts
it('refreshes runtime status from the selected runtime', async () => {
  const store = createAppStore({
    ...createRuntimeStub(),
    getEnvironmentStatus: async () => ({
      mode: 'tauri',
      label: '桌面运行时',
      available: false,
      checkedAt: '2026-05-06T00:00:00.000Z',
      issues: [{ code: 'runtime-directory-readonly', message: '目录不可写', recoverable: true }]
    })
  })

  await store.refreshRuntimeStatus()

  expect(store.runtimeStatus.value?.mode).toBe('tauri')
  expect(store.operation.value.lastError?.message).toBe('目录不可写')
})

it('blocks generation when runtime environment validation fails', async () => {
  const store = createAppStore({
    ...createRuntimeStub(),
    validateEnvironment: async () => [{ code: 'runtime-directory-readonly', message: '目录不可写', recoverable: true }]
  })
  store.projectTitle.value = '智慧仓储管理系统 V1.0'
  store.settings.value.basic.outputDirectory = 'D:/readonly'
  store.settings.value.agent = { baseUrl: 'https://api.example.com', apiKey: 'sk-demo', model: 'demo-model', status: 'available' }
  store.addTemplate('说明书.docx', 'software-manual')
  await store.parseTemplate(store.settings.value.templates[0].id)

  const started = await store.startGeneration()

  expect(started).toBe(false)
  expect(store.operation.value.lastError?.code).toBe('runtime-directory-readonly')
  expect(store.currentTask.value).toBeNull()
})
```

Also add helper imports and helper function:

```ts
import type { GenerationRuntime } from '../runtime/types'
import { createMockRuntime } from '../runtime/mockRuntime'

function createRuntimeStub(): GenerationRuntime {
  return createMockRuntime()
}
```

- [ ] **Step 2: Run Store tests and confirm failure**

Run:

```bash
npm run test -- src/stores/appStore.test.ts
```

Expected: FAIL because `runtimeStatus` and `refreshRuntimeStatus` do not exist and `startGeneration` does not call runtime validation.

- [ ] **Step 3: Refactor Store default runtime and status**

Update imports in `src/stores/appStore.ts`:

```ts
import { createDefaultRuntime } from '../runtime/runtimeFactory'
import type { GenerationRuntime, RuntimeEnvironmentStatus } from '../runtime/types'
```

Change `createAppStore` signature:

```ts
export function createAppStore(runtime: GenerationRuntime = createDefaultRuntime()) {
```

Add state:

```ts
const runtimeStatus = ref<RuntimeEnvironmentStatus | null>(null)
```

Add action:

```ts
async function refreshRuntimeStatus(): Promise<boolean> {
  const status = await runtime.getEnvironmentStatus(settings.value)
  runtimeStatus.value = status
  operation.value.lastError = status.issues[0] ?? null
  return status.available
}
```

- [ ] **Step 4: Add runtime validation before generation**

Inside `startGeneration()`, after existing `validateGenerationStart` passes and before `operation.value.generating = true`, add:

```ts
const environmentIssues = await runtime.validateEnvironment(settings.value)
if (environmentIssues.length > 0) {
  operation.value.lastError = environmentIssues[0]
  runtimeStatus.value = await runtime.getEnvironmentStatus(settings.value)
  currentTask.value = null
  storage.remove('currentTask')
  return false
}
```

Return `runtimeStatus` and `refreshRuntimeStatus` from the store object.

- [ ] **Step 5: Run Store verification**

Run:

```bash
npm run test -- src/stores/appStore.test.ts src/runtime/tauriRuntime.test.ts
npm run build
```

Expected: PASS.

- [ ] **Step 6: Commit Store runtime status integration**

Run:

```bash
git add src/stores/appStore.ts src/stores/appStore.test.ts
git commit -m "feat: track runtime environment status"
```

## Task 4: Settings Runtime Status UI

**Files:**
- Modify: `src/components/SettingsView.vue`
- Modify: `src/components/SettingsView.test.ts`
- Modify: `src/styles.css`

- [ ] **Step 1: Add failing Settings UI tests**

Add to `src/components/SettingsView.test.ts`:

```ts
it('shows runtime mode and refresh action', async () => {
  const store = createAppStore()
  await store.refreshRuntimeStatus()
  const wrapper = mount(SettingsView, { props: { store } })

  expect(wrapper.text()).toContain('浏览器模拟运行时')
  expect(wrapper.find('[data-test="refresh-runtime-status"]').exists()).toBe(true)
})

it('shows runtime environment errors', async () => {
  const store = createAppStore()
  store.runtimeStatus.value = {
    mode: 'tauri',
    label: '桌面运行时',
    available: false,
    checkedAt: '2026-05-06T00:00:00.000Z',
    issues: [{ code: 'runtime-directory-readonly', message: '目录不可写', recoverable: true }]
  }
  const wrapper = mount(SettingsView, { props: { store } })

  expect(wrapper.text()).toContain('桌面运行时')
  expect(wrapper.text()).toContain('目录不可写')
})
```

- [ ] **Step 2: Run Settings tests and confirm failure**

Run:

```bash
npm run test -- src/components/SettingsView.test.ts
```

Expected: FAIL because the runtime status section is not rendered.

- [ ] **Step 3: Update SettingsView**

Add a runtime section after the basic information section in `src/components/SettingsView.vue`:

```vue
<section class="settings-section runtime-section">
  <header>
    <h2>运行环境</h2>
    <button
      data-test="refresh-runtime-status"
      type="button"
      @click="store.refreshRuntimeStatus"
    >
      重新检测
    </button>
  </header>
  <div class="runtime-status">
    <span class="status-pill">
      {{ store.runtimeStatus.value?.label ?? '未检测' }}
    </span>
    <span class="muted-text">
      最近检测：{{ store.runtimeStatus.value?.checkedAt ?? '未检测' }}
    </span>
  </div>
  <p
    v-for="issue in store.runtimeStatus.value?.issues ?? []"
    :key="issue.code"
    class="inline-error"
  >
    {{ issue.message }}
  </p>
</section>
```

- [ ] **Step 4: Add CSS**

Add to `src/styles.css`:

```css
.runtime-section .runtime-status {
  display: flex;
  flex-wrap: wrap;
  gap: 8px 12px;
  align-items: center;
  min-height: 32px;
}
```

- [ ] **Step 5: Run Settings verification**

Run:

```bash
npm run test -- src/components/SettingsView.test.ts src/stores/appStore.test.ts
npm run build
```

Expected: PASS.

- [ ] **Step 6: Commit Settings runtime UI**

Run:

```bash
git add src/components/SettingsView.vue src/components/SettingsView.test.ts src/styles.css
git commit -m "feat: show runtime environment in settings"
```

## Task 5: Docs, Chinese Naming, and Final Verification

**Files:**
- Rename: `docs/superpowers/specs/2026-05-06-soft-copyright-tauri-runtime-minimum-design.md` -> `docs/superpowers/specs/2026-05-06-真实桌面运行时最小接入设计.md`
- Modify: `README.md`
- Modify: `docs/superpowers/plans/2026-05-06-真实桌面运行时最小接入.md`

- [ ] **Step 1: Rename spec to Chinese file name**

Run:

```bash
git mv docs/superpowers/specs/2026-05-06-soft-copyright-tauri-runtime-minimum-design.md docs/superpowers/specs/2026-05-06-真实桌面运行时最小接入设计.md
```

Expected: the spec file keeps the same content and now follows `AGENTS.md` document naming.

- [ ] **Step 2: Update README runtime section**

Update `README.md` runtime section to include:

```md
当前版本通过 `createDefaultRuntime()` 自动选择运行时：浏览器环境使用 `mockRuntime`，检测到桌面能力后可切换到最小 `tauriRuntime`。

`tauriRuntime` 当前只负责运行模式识别和产出资源目录环境校验；Agent、docx 解析、真实文件落盘和 zip 输出仍保留为后续接入点。
```

- [ ] **Step 3: Run full verification**

Run:

```bash
npm run test
npm run build
git status --short
```

Expected:

- `npm run test` passes all test files.
- `npm run build` passes TypeScript and Vite production build.
- `git status --short` only shows README, this plan, and spec rename before final commit.

- [ ] **Step 4: Mark plan complete**

Replace all actual task checkboxes in this file from `[ ]` to `[x]`. Keep the header example unchanged.

- [ ] **Step 5: Commit final docs**

Run:

```bash
git add README.md docs/superpowers/plans/2026-05-06-真实桌面运行时最小接入.md docs/superpowers/specs/2026-05-06-真实桌面运行时最小接入设计.md
git commit -m "docs: document minimal desktop runtime integration"
```

## Final Acceptance

Run:

```bash
npm run test
npm run build
git status --short --branch
```

Expected:

- Tests pass.
- Build passes.
- Worktree is clean on `feature/front-prototype`.
- Browser mode still uses mock runtime.
- Settings page shows runtime mode and environment issues.
